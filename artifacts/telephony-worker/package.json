{
  "name": "@workspace/telephony-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js",
    "test": "pnpm run build && node --test dist/worker.test.js"
  },
  "devDependencies": { "@types/node": "catalog:" }
}